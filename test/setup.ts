/**
 * Global test setup.
 *
 * `@testing-library/jest-dom` adds DOM matchers (toBeInTheDocument, etc.) that
 * only take effect in jsdom-environment test files; importing it here is inert
 * under the node environment, so it is safe as a global setup file.
 */
import '@testing-library/jest-dom/vitest'
